extends layout

block content
    div(class="mt-12")
      if(message)
        h1=message
      if(error)
        if(error.status)
            h2=error.status
        if(error.stack)
            pre #{error.stack}
